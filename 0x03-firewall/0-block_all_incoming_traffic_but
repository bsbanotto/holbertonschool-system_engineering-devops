# Set incoming/outgoing defaults
sudo ufw default allow outgoing
sudo ufw default deny incoming

# Set to allow incoming on ports 22, 80, 443
sudo ufw allow SSH
sudo ufw allow HTTPS SSL
sudo ufw allow HTTP

# Enable ufw
sudo ufw enable